# pg_dbwa extension

comment = 'PostgreSQL Database workload analizer'
default_version = 0.2

requires = 'dblink, pg_stat_statements, pg_eyes, pg_prttn_tools'

encoding = utf8
superuser = true
relocatable = false
schema = dbwa
